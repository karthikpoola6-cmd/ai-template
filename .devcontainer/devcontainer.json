{
  "name": "Codespaces AI Template - Bootstrap",
  "image": "mcr.microsoft.com/devcontainers/python:3.12",

  "features": {
    "ghcr.io/devcontainers/features/common-utils:2": {}
  },

  "postCreateCommand": "pip install pyyaml && echo '' && echo '━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━' && echo '  Codespaces AI Template - Ready to Bootstrap' && echo '━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━' && echo '' && echo '  Get started:' && echo '    ./bootstrap.sh          # Interactive setup' && echo '    ./bootstrap.sh --quick  # Quick start (Python only)' && echo '    ./bootstrap.sh --help   # See all options' && echo '' && echo '  After bootstrap:' && echo '    make doctor             # Verify setup' && echo '    make info               # View configuration' && echo '    make help               # See all commands' && echo ''",

  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "github.copilot",
        "ms-vscode.makefile-tools"
      ],
      "settings": {
        "python.defaultInterpreterPath": "/usr/local/bin/python"
      }
    }
  },

  "remoteUser": "vscode"
}
